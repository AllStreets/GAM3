# HYPERION — Project Handoff

*Written 2026-08-23 for the next Claude session. Read this first, then `docs/superpowers/specs/2026-08-22-hyperion-design.md` (the binding spec).*

## The Vision (one paragraph)

HYPERION is Connor's game: command a satellite fleet over a **cinematic, living, REAL Earth**. Real orbital mechanics you fly by hand. Real world events (earthquakes, storms, wildfires, launches) from live feeds are the content. An LLM turns those events + your personal playstyle into briefings, missions, and eventually storylines/rivals — so **the game is different for every player and grows with them**. Pillars in priority order: gorgeous graphics, cool physics, excitement, per-user AI divergence. Story is ambient flavor, never a gate.

## Where We Are (Plans 1–12 shipped, live at https://hyperion-flax.vercel.app)

| Plan | Delivered |
|---|---|
| 12 — Living World (away-tick) | **The world evolves while you're offline.** A Vercel cron (`vercel.json`, `*/20 * * * *`) hits a `CRON_SECRET`-protected `/api/tick` (GET) that iterates *offline* players in Neon (`dueOwnersForTick` — skips live sessions via 300s `updated_at` + `lastTickSim` gate) and advances each via the **pure, deterministic** `src/lib/worldTick.ts` `advanceWorld(state, {nowSim,events,periodSec})`: active contracts expire (−5 rep), the **rival claims** contested races whose ETA passed (−1 + `applyRaceResult`), fresh **reachability-filtered** contracts are seeded, and story arcs advance — rules **match live `evaluate`** so offline == online. Results + a `WorldDigest` are written back (`src/lib/db.ts` `loadOwnerState`/`writeOwnerState`, merge-preserving). `src/lib/eventsSource.ts` (`fetchWorldEvents`) is shared by `/api/events` (unchanged) + the tick. On return, `PersistBridge` loads `hyperion-worlddigest-v1` → `coldOpen.ts` `digestLines` → `ColdOpenScreen` **"WHILE YOU WERE AWAY"** (⚔ rival claims, N expired, N new contracts, arc beat) — display-only, **no double-apply** (client adopts server state; `evaluate` only touches `active`). ALWAYS-safe: per-owner try/catch, disabled when no `CRON_SECRET`/DB, build works with no DB. opus security review APPROVED. **⚠ ACTION: Connor must add `CRON_SECRET` in Vercel (`vercel env add CRON_SECRET production` + preview) for the deployed cron; until then `/api/tick` self-disables (game unaffected).** Plan: `docs/superpowers/plans/2026-08-26-hyperion-12-living-world-tick.md`. |
| 11 — Persistence | **Cloud saves + optional accounts.** Game state persists server-side in **Neon Postgres** (`src/lib/db.ts` — `@neondatabase/serverless`, a `saves(owner_id, store_key, data jsonb)` table, `isStoreKey` allowlist) via `/api/save` + `/api/load` + `/api/migrate` (ALWAYS-200, parameterized SQL, **build works with no DB**). A client bridge (`src/lib/anonId.ts` + `src/components/PersistBridge.tsx`) hydrates stores from the server on load and debounce-writes changes, **falling back to localStorage on any failure** (controller-verified: state survives a localStorage wipe, restored from Neon). **Optional Clerk sign-in** (Clerk v7 Core 3 on Next 16 — **middleware is `src/proxy.ts`**, and `<SignedIn>`/`<SignedOut>` are gone → use `useAuth().isSignedIn`): `ClerkProvider` in the root layout, `clerkMiddleware()` protects **no** route (play fully signed-out via anon id), routes derive `owner_id = (await auth()).userId ?? x-anon-id` (server-trusted, unforgeable), and anonymous progress **migrates into the account on first sign-in** (`migrateSaves`, no-clobber). `SettingsPanel` (⚙) adds AUDIO mute, an ACCOUNT section (SIGN IN / user + SIGN OUT), a saved-status indicator (synced / on-device / offline via `persistStatus`), and a **bulletproof RESET** (clears all keys + reloads atomically). opus security review: owner isolation server-enforced, no cross-user access, secrets server-only + uncommitted, login-optional + degrade-to-local. Env (Neon + Clerk) already in the Vercel `hyperion` project. Plan: `docs/superpowers/plans/2026-08-26-hyperion-11-persistence.md`. **Next options:** server-authoritative living-world ticks (contracts/story/rival advance while away + GDACS ingestion), satellite fidelity pass, or Connor's next feature. |
| 10 — Resilience & Progression | **Never a dead end.** (From a playtest soft-lock: a committed satellite couldn't reach its contract and refuel was unaffordable.) **Reachability** (`src/lib/reachability.ts`): auto-generated contracts (briefing/story/seed) are dropped if no satellite's orbital plane can bring its ground-track over the target (`|lat| ≤ inclination + 4.5°`, retrograde folded) — never-blank preserved; **player click-to-create is unrestricted** (tagged "beyond coverage"). Reach chips green/amber/red on every contract. **Recovery:** `STAND DOWN` (two-step confirm) frees a committed satellite; **partial refuel** (`refuelSatellite(id, dv?)` buys the affordable amount; full-refuel price unchanged at efficiency 0). **Earned safety net** (`src/lib/milestones.ts`): every 5th completion grants a **relief grant** + an **emergency-refit token** (free refuel of one stranded sat); a guarded **stuck backstop** (§150, once per stuck episode) is the hard floor. **Upgrades** (`src/lib/upgrades.ts`, `UpgradesPanel`): bigger tanks (`tankLevel`/`fuelCapacity`), refuel efficiency (agency-wide, floor-capped), capability retrofit; **buy-a-satellite-aimed** (`src/lib/aimPlane.ts` `planeForTarget` → inclination ≥ |targetLat|, `buySatelliteAimed`, `BuyPlanePicker`) so you can purchase coverage for a hard target. Legible disabled-reason states throughout. **Economy airtight** (single completion award untouched, all spends via `spendFunding`, no overspend/exploit). Spec §10: `docs/superpowers/specs/2026-08-25-hyperion-resilience-progression-design.md`. |
| 9 — Story Engine | **The world has memory + drama.** Completing a contract over a place **blooms its real city image** into the 7B completion cinematic (progressive-enhancement, race-guarded — §8.2 fold). **More real feeds:** GDACS disasters (`src/lib/gdacs.ts`), NOAA SWPC space-weather (`src/lib/spaceWeather.ts`), wider EONET categories → new `EventKind`s volcano/flood/spaceweather (wired through `contractMeta`/`eventIcons`/`EventLayer`; all keyless, ALWAYS-200, isolated). **Contract objective variety** (`src/lib/contractObjective.ts`): single-pass / multi-pass / multi-satellite / **dwell**, with live progress in ContractsPanel and `objectiveRewardScale` folded into the SINGLE capped (§1500) funding award. **AI story engine:** `storyStore` (arcs + `Dispatch`s + rival, persisted) + `/api/story` (claude-opus-4-8, ALWAYS-200, **strips event titles from the prompt** = injection-safe, respectful) + `DispatchesFeed` (left rail); triggered on session + completion, debounced. **Rival agency with memory** (`src/lib/rival.ts`, e.g. "VANTIS Corp"): occasionally (~1-in-4, non-place) **contests** a contract — a race whose outcome the **engine adjudicates deterministically** (`rivalEtaSec` from acceptedAt vs simTime), LLM only narrates taunts; win = normal reward + a beat, rival-claim = gentle −1 rep soft-fail. Dwell/multi-pass dt from the real sim-time delta. Spec §9 + §8.2. |
| 8 — Living Places | **The globe is a place you go now.** Click any point on Earth → a **Place Card** (bottom-center): nearest-city name (`src/data/cities.ts` ~210 cities + `src/lib/nearestCity.ts`, keyless/offline), nearby live events, the fitting archetype/capability, and a **real city photo**. **Cinematic city reveal** — `GlobeEngine` camera **push-in from orbit** + real image bloom (`CityRevealOverlay`), via keyless **`/api/place-image`** (Wikimedia Commons geosearch → curated fallback `src/data/cityImages.ts` → stylized; ALWAYS-200, cached, attribution shown). **Click-to-generate AI contracts** grounded in the clicked location (**`/api/place-contract`**, engine sets target/reward/deadline, the model only flavors; prompt hardened so it never drifts to distant events; ALWAYS-200 fallback). **Postcards front-and-center** — city-image hero, named-by-place downloads, session `PostcardStrip`. Contract **objectives now shown**. Globe-pick keeps satellite priority + is gated `founded && !burnSession`; reveal beat sits below burn/completion in the camera authority order with ease-back on dismiss. Spec §8: `docs/superpowers/specs/2026-08-24-hyperion-living-world-design.md`. |
| 1 — Globe | Three.js cinematic Earth: real UTC terminator (shader), city lights, clouds, atmosphere, starfield, bloom+grain, intro sweep, HUD shell |
| 2 — Orbits | Real Keplerian mechanics (`src/lib/orbits.ts`, server-portable, heavily unit-tested), 2-satellite fleet, zustand `gameStore`, burn planner with live ghost-orbit preview, sim time 20× (`src/lib/simTime.ts`, anchored 2026-01-01) |
| 3 — Living world | USGS/EONET/LaunchLibrary → `/api/events` (timeouts, per-source isolation), `worldStore` (never-blank rule), animated `EventLayer`, EventsPanel with camera fly-to |
| 4 — Game feel | Flying the burn: IGNITE → chase cam → HOLD SPACE + A/D trim → quality scales fuel overspend (cost×1.25 arm margin). Synthesized WebAudio (`src/audio/AudioEngine.ts` — ambient/chirps/rumble/stinger, zero assets), screen shake, BurnOverlay |
| 5 — Visuals | Procedural 3D satellites (gold bus, solar wings, dish, nav blink; sun-tracking via `group.up`; DirectionalLight follows terminator), canvas-drawn event icon sprites (seismic/flame/cyclone/rocket) |
| 6 — AI v1 | Playstyle profile (`src/lib/profile.ts`, localStorage, tracks burns/quality/aborts/focus-kinds/sessions) → `/api/briefing` (claude-opus-4-8, `messages.parse` + zod structured outputs, effort low, respectful-framing system prompt, body validation, ALWAYS-200 fallback) → BriefingPanel with per-player missions on REAL events + TRACK buttons |
| 7B — Soul & spectacle | **The game has soul now.** Satellites carry callsigns, a **capability** (imaging/comms/thermal → `CAPABILITY_LABEL`) and a **service record** (contracts completed, sim-days in orbit, notable passes), and can be **permanently lost** (never-ruin: a free provisional replacement if the fleet would hit zero). Agencies have a hidden **3-axis archetype leaning** (`src/lib/archetype.ts`, relief/research/defense) → an evolving title, a biased contract stream, and an **archetype-flavored `/api/briefing`** (geopolitical/nature/military-tech story lanes; still ALWAYS-200 + respectful + engine-enforced). Contracts carry an archetype tag + preferred capability → **capability-match funding bonus**. Every burn is **scored** (efficiency/precision → S/A/B/C, `src/lib/maneuverScore.ts`) with **orbital trick-shots** (one pass ≥2 targets). Completions are a **cinematic set-piece** (`CompletionFx` chase-lock + `CompletionCinematic`: scanning sweep, downlink count-up, reward tally, operational-tempo streak multiplier, respectful relief-impact line). **Debris-conjunction emergencies** (`src/lib/emergency.ts`, `EmergencyAlert`) demand an evasive burn or you lose the bird (rare: ~4–5 wall-min gap). Returning players get a **"while you were away" cold-open** (`src/lib/coldOpen.ts`); new players get a **first-run walkthrough** + illustrated `GuidePanel`; anyone can capture a shareable **orbital postcard** (`src/lib/postcard.ts`). Clickable **service-record cards**; shared `ui/Chip`. All client-side/localStorage, back-compat with pre-7B saves. Spec §§1b/6/6b/7/7b/8/9/9b/9c/10/12. |
| 7A.5 — Legibility & pacing | The core loop now teaches itself (from a controller playtest that found 7A worked but was confusing). `GuidanceHint` coach line (accept → ① select sat → ② drag NORMAL to green → ③ IGNITE); FleetPanel shows each satellite's closest-approach to the target + "◀ best" badge; InterceptReadout gives a **directional hint** ("nudge NORMAL ◀/▶", from `normalHint`) + **near-term pass ETA** (`firstPassEta`); events list collapsed by default; deadlines 3→5 periods; burn sliders widened ±400→±800 so far contracts are one-burn winnable. Verified end-to-end: a fresh player is guided to complete a contract. |
| 7A — Game foundations | **It's a game now.** Found an agency (name + procedural SVG emblem + colorway, `FoundingScreen`); 5-satellite fleet w/ diverse planes; contracts from AI briefing (`contractStore`, accept→active→complete/expire); **maneuver-to-intercept** — fly a burn to bring a satellite's ground-track over a real-event target (`src/lib/intercept.ts` closest-approach solver; `ContractLayer` target ring + ground-track + marker; `InterceptReadout` ghost-aware closest-approach HUD); lean economy (`agencyStore` funding+reputation, refuel, buy-satellite); CONTRACTS + AGENCY panels (right-rail flex stack); field GUIDE (`?`); effect-based store hydration (reload-safe); all client-side localStorage. Spec: `docs/superpowers/specs/2026-08-23-hyperion-game-layer-design.md` |

**Gates:** 511 unit tests, 25 e2e (Playwright, includes console-error + non-black-canvas + founding/walkthrough/guide + place-card/city-reveal + story-engine + resilience), tsc clean, production build OK. **Infra:** GitHub `AllStreets/GAM3` → Vercel project `hyperion` auto-deploys main; `ANTHROPIC_API_KEY` in Vercel prod+preview env (Sensitive) and `.env.local` (gitignored; verified never committed). **Port 3100 — never 3000 (AgentZeus owns 3000).**

## Connor's Direct Feedback (2026-08-23 — the priorities)

1. **OBJECTIVES ARE THE GAP.** "Games need objectives." Plan 6's briefing missions are the seed, but they're advisory-only — no acceptance, completion detection, rewards, or economy. This is the #1 next thing: make missions real (accept → satisfy conditions e.g. imaging pass over the event within N orbits → earn funding/reputation → spend on fleet).
2. **Storylines matter** — rival agencies with memory, arcs that escalate from the profile (spec's "fiction weaver"). Build after/with objectives.
3. **Satellites: "V1 good, we can do better"** — more detail/fidelity on the 3D models (textures, greebles, better materials, maybe per-satellite variants).
4. Standing directive: keep building autonomously, polished beautiful product; pause only for credentials/irreversible decisions.

## Roadmap (agreed + spec-derived)

The game-layer spec (`docs/superpowers/specs/2026-08-23-hyperion-game-layer-design.md`) split into 7A (DONE) and **7B (DONE)**. Both shipped and live.

0. **Balance/pacing still to tune** (carried, some now easier post-7B): **reachability-aware contract generation** — offer/annotate contracts by whether some satellite can reach them within an affordable single burn (the "◀ best" bird can still be >800 m/s away on hard geography — the archetype-biased stream helps but doesn't fully solve); **economy balance** — a full refuel (~§1080) can exceed a single base reward (~§300–500), though the 7B capability-match bonus + streak multiplier now push winnings up. **7B follow-ups noted in final review (non-blocking):** the cinematic + emergency overlays were verified by unit tests + code review + clean mount, but NOT live-rendered end-to-end (would need a flythrough or a dev store-injection hook) — worth a real playtest; `priorSession.ts` cold-open capture relies on module-import ordering (documented, correct today, but fragile if mounts reorder).
1. **Plan 8 — Living Places (DONE, shipped & live):** click-to-inspect Place Card, cinematic city reveal (push-in + real image bloom), keyless `/api/place-image`, click-to-generate `/api/place-contract`, postcards front-and-center. Spec `docs/superpowers/specs/2026-08-24-hyperion-living-world-design.md` §8. **Plan 8 follow-ups (non-blocking, from reviews/playtest):** the §8.2 completion-fold (city image blooming in at a contract completion) was deferred — no wiring in `CompletionFx` yet; some curated `cityImages.ts` URLs are unverified (degrade gracefully); reveal zooms to 1.25R (below controls.minDistance 1.4R) with an ease-back on dismiss.
2. **Plan 9 — Story Engine (DONE, shipped & live):** completion city-image bloom (§8.2), GDACS + NOAA space-weather + more EONET categories, contract objective variety (multi-pass/multi-sat/dwell), AI story arcs + dispatches (`/api/story`), and a rival agency with memory (engine-adjudicated races). Spec §9. **Plan 9 non-blocking follow-ups (from final review):** `storyProgress.pickArcTheme` tie-break compares against initial `''` (cosmetic); feed `time` strings not ISO-validated before sort (never crashes); `difficulty = reward.reputation/30` is a magic proxy. **Also fixed this session:** globe **geolocation** — the Earth texture was 90° off the `latLonToVector3` frame; rotated the textured shells −90° so clicks/markers/terminator align (engine change → needs a full dev-server restart to pick up); postcard/guide button overlap.
3. **Plan 10 — Resilience & Progression (DONE, shipped & live):** reachability filter, stand-down, partial refuel, earned relief grants + refit tokens + stuck backstop, four upgrades, buy-aimed. Spec §10. **Non-blocking follow-ups (final review):** `planeForTarget` inclination guarantee breaks for |lat|>~88.5° (no real targets there); the `standDown` stuck-heuristic is a local approximation separate from `isAgencyStuck` (two "stuck" defs — could unify).
4. **Plan 11 — Persistence (IN PROGRESS):** Neon + Clerk **provisioned** (env in `.env.local` + Vercel `hyperion`). Plan: `docs/superpowers/plans/2026-08-26-hyperion-11-persistence.md`. **T1+T2 DONE + live:** Neon `saves` table + `db.ts` + `/api/save`+`/api/load` (anon `x-anon-id`, ALWAYS-200, injection-safe, builds w/o DB) + `anonId.ts` + `PersistBridge.tsx` (server hydrate + debounced write-through, localStorage fallback). Controller-verified: state survives a localStorage wipe (restored from Neon). Login NOT required. **REMAINING: T3** (Clerk `@clerk/nextjs` — ClerkProvider + middleware + Settings sign-in; routes owner = `auth().userId` else x-anon-id; `migrateSaves(anon→user)` on sign-in) + **T4** (account-vs-device saved status; resilience audit; ensureSchema `schemaReady` flag; e2e; gate). Implementers: consult vercel `auth`+`vercel-storage` skills + `node_modules/next/dist/docs/`. Also shipped: a **Settings panel** (⚙, `SettingsPanel.tsx`) with audio mute + a reliable RESET (clears all keys + reloads atomically). Server-authoritative living-world ticks + GDACS ingestion come after persistence lands.
5. **Satellite fidelity pass** + more flying moments (re-entry corridor spec'd, client-only) + polish backlog below.

## How This Project Is Built (process that's been working)

Write plan to `docs/superpowers/plans/` (full code in plan, no placeholders) → superpowers subagent-driven-development (implementer subagent per task + reviewer per task or combined for small branches + final fable/opus review + ONE fix wave) → controller visually verifies via Playwright MCP screenshots on :3100 → merge to main → push (auto-deploys). Ledger in `.superpowers/sdd/<plan>/progress.md` during execution. Memory in `~/.claude/projects/-Users-connorevans-Downloads-GAM3/memory/`.

## Architecture Invariants (do not break)

- Scene frame: Y-up, Earth-fixed, non-rotating Earth (terminator moves via shader). `latLonToVector3` (geo.ts) ↔ `sceneFromEci` (orbits.ts). Orbital math is pure/deterministic: positions from (elements, epoch, t) — the living world needs no frame simulation, only discrete events.
- Engine (`src/engine/`) never imports React; React never touches Three objects — zustand stores bridge (`getState()`/`subscribe` from engine; non-reactive hot fields like `previewAt`/`burnLive` are direct-mutated, never `set()`).
- Camera authority order in `GlobeEngine.update()`: intro sweep → event flight → burn chase (wins) → shake last.
- LLM: server routes only, structured outputs (zod), ALWAYS-200 with deterministic fallback, respectful real-event framing (never gamify casualties — missions on real disasters are observe/map/relay/support only). Engine enforces what the LLM proposes (e.g. eventId whitelist).
- Feeds: per-source isolation, 8s timeouts, never blank a non-empty world.
- Game feel is policy: every mechanic visible, audio on interactions, juice.

## Known Deferred Minors (ride-alongs, in old ledgers/reviews)

Sprite limb-clipping at grazing angles; dayside additive washout; event icons small at full-globe zoom (severity-scaled sizing is the obvious fix); `dvSpent` records planned not actual cost; per-frame Vector3 allocations (fine at current scale, pool before fleet grows); EventLayer full rebuild per poll is now identity-guarded but still O(n) on real changes; SatelliteLayer rebuild same; lookAt near-degeneracy jitter when orbit plane contains sun vector; `simNow` single funnel must hold for future server-authoritative time.
